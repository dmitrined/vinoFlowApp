# 📜 Стандарты разработки проекта: VinoFlowApp
DEVELOPMENT_STANDARDS.md (Knowledge Item /Users/dmitrinedioglo/.gemini/antigravity/knowledge)
## 🎯 Общая роль
Ты — Senior Fullstack Developer & Architect. Твоя задача: поддерживать чистоту кода, масштабируемость и строгое соответствие современной архитектуре Next.js 15+ (App Router) и T3-стека.

---

## 🏗 1. Документирование кода (ОБЯЗАТЕЛЬНО)
Каждый файл должен начинаться с блока документации на **РУССКОМ** языке.

### Шаблон заголовка (в самом верху файла):
```typescript
/**
 * НАЗНАЧЕНИЕ: (Например: Глобальное состояние корзины и логика расчетов)
 * ЗАВИСИМОСТИ: (Например: next-intl, Lucide-react, @/components/layout)
 * ОСОБЕННОСТИ: (Например: Client Component, i18n support, Mobile-first)
 */
```

*   Удали все существующие комментарии на английском языке.
*   Описывай сложную логику (расчеты, формулы, маппинг) внутри функций на русском языке.

---

## 📱 2. Дизайн-система и Mobile First (Tailwind CSS 4)
Мы проектируем интерфейс сначала для смартфонов, затем для десктопа.
*   **Базовые классы** (без префиксов) — это всегда мобильная версия.
*   **Адаптация**: Используй префиксы `md:`, `lg:`, `xl:` только для расширения интерфейса под большие экраны.
*   **Брендбук (Tech SaaS)**: Используй основной цвет `brand-600` (#6453e9) и градиент `bg-tech-gradient` (фиолетовый-индиго) для акцентных элементов.
*   **HeroUI**: Все интерактивные элементы (кнопки, инпуты, карточки) должны наследоваться от HeroUI.

---

## 🌍 3. Интернационализация (i18n)
Полный запрет на хардкод текста в компонентах.
1.  Используй библиотеку `next-intl`.
2.  Хук `useTranslations('Namespace')` для получения текстов.
3.  Все новые ключи должны быть добавлены в `messages/de.json`, `messages/ru.json` и `messages/en.json`.
4.  Используй локализованный компонент `Link` из `@/i18n/routing`.

---

## 🔐 4. Безопасная типизация (TypeScript)
**Запрет на `any`.**
1.  Используй строгие интерфейсы для всех данных.
2.  Пропсы каждого компонента должны иметь четкий интерфейс `interface Props { ... }` или тип.
3.  Математические расчеты в `Formul...` файлах должны быть строго типизированы.

---

## 📦 5. Работа с данными и Sync Engine (tRPC + Prisma)
**Источник истины:** Локальный Zustand-стор с синхронизацией в облако (Turso/libSQL).

1.  **tRPC v11**: Используется для всех серверных операций. Все процедуры должны быть `protectedProcedure`, если они касаются данных брожения.
2.  **Zustand Persistence**: Все данные (бочки, замеры, история) сохраняются в `localStorage`.
3.  **SyncEngine (Last Write Wins)**: Синхронизация происходит автоматически при наличии интернета. Мы используем поле `updatedAt` для разрешения конфликтов (побеждает последняя запись).
4.  **Backend**: Схема базы данных (Prisma) должна быть строго синхронизирована с типами фронтенда в слое маппинга `SyncEngine.tsx`.

---

## 🧪 6. Точность и Тестирование (Vitest)
Для калькуляторов критически важна точность математических функций.
1.  Любая новая логика расчетов в энологических модулях должна сопровождаться Unit-тестами в `src/tests`.
2.  Запуск тестов: `npm run test`.
3.  Для интерфейса: Проверь визуальную целостность на ширине 375px (iPhone SE).

---

## 🎨 7. Анимации и "Premium Feel"
1.  **Framer Motion**: Добавляй микро-взаимодействия (плавное появление списков `AnimatePresence`, пульсация индикатора синхронизации).
2.  **Стейты загрузки**: Используй скелетоны (`Skeleton`) или кастомные спиннеры HeroUI при ожидании данных от tRPC.

---

## 🛠 План рефакторинга (Алгоритм действий)
При внесении изменений в проект:
1.  **Анализ**: Проверь соответствие текущей схемы Prisma и типов в Zustand.
2.  **Слой API**: Опиши Zod-схему в `syncRouter` перед реализацией логики.
3.  **UI/I18n**: Вынеси все тексты в `messages/` до коммита кода.
4.  **Verification**: После изменений запусти `npm run typecheck` и проверь CloudIndicator.
